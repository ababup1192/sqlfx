// GENERATED by flix_db gen v1 from migrations. Do not edit.

///
/// posts テーブル。enum は断片 DSL の型に付ける印、関数は列。
///
mod PostsTable {

    use SqlValue.SqlValue

    pub enum PostsTable {
        case PostsTable
    }

    pub def id(): Fragment.Col[PostsTable, Int64] = Fragment.Col.Col("id", SqlValue.Int64)
    pub def userId(): Fragment.Col[PostsTable, Int64] = Fragment.Col.Col("user_id", SqlValue.Int64)
    pub def title(): Fragment.Col[PostsTable, String] = Fragment.Col.Col("title", SqlValue.Str)
    pub def body(): Fragment.Col[PostsTable, String] = Fragment.Col.Col("body", SqlValue.Str)
    pub def published(): Fragment.Col[PostsTable, Bool] = Fragment.Col.Col("published", SqlValue.Bool)
    pub def tags(): Fragment.Col[PostsTable, List[String]] = Fragment.Col.Col("tags", SqlValue.StrArray)
    pub def createdAt(): Fragment.Col[PostsTable, Int64] = Fragment.Col.Col("created_at", SqlValue.Timestamp)
    pub def views(): Fragment.Col[PostsTable, Int32] = Fragment.Col.Col("views", SqlValue.Int32)

}

///
/// users テーブル。enum は断片 DSL の型に付ける印、関数は列。
///
mod UsersTable {

    use SqlValue.SqlValue

    pub enum UsersTable {
        case UsersTable
    }

    pub def id(): Fragment.Col[UsersTable, Int64] = Fragment.Col.Col("id", SqlValue.Int64)
    pub def name(): Fragment.Col[UsersTable, String] = Fragment.Col.Col("name", SqlValue.Str)
    pub def email(): Fragment.Col[UsersTable, String] = Fragment.Col.Col("email", SqlValue.Str)
    pub def role(): Fragment.Col[UsersTable, String] = Fragment.Col.Col("role", SqlValue.Str)
    pub def createdAt(): Fragment.Col[UsersTable, Int64] = Fragment.Col.Col("created_at", SqlValue.Timestamp)
    pub def deletedAt(): Fragment.Col[UsersTable, Int64] = Fragment.Col.Col("deleted_at", SqlValue.Timestamp)

}
