// GENERATED by sqlfx gen v10 from migrations. Do not edit.

///
/// Table posts. The enum is the tag the fragment DSL puts on the type; the functions are the columns.
///
mod PostsTable {

    use Sqlfx.Date
    use Sqlfx.DbErr
    use Sqlfx.DbError
    use Sqlfx.DbRead
    use Sqlfx.DbWrite
    use Sqlfx.Decoder
    use Sqlfx.Fragment
    use Sqlfx.RawSql
    use Sqlfx.Sql
    use Sqlfx.SqlValue
    use Sqlfx.Timestamp
    use Sqlfx.TransientDbErr
    use Sqlfx.Tx
    use Sqlfx.Uuid
    use Sqlfx.SqlValue.SqlValue
    use Sqlfx.Decoder.Decoder

    pub enum PostsTable {
        case PostsTable
    }

    pub def id(): Fragment.Col[PostsTable, Int64, Fragment.NotNull] = Fragment.Col.Col("id", SqlValue.Int64)
    pub def userId(): Fragment.Col[PostsTable, Int64, Fragment.NotNull] = Fragment.Col.Col("user_id", SqlValue.Int64)
    pub def title(): Fragment.Col[PostsTable, String, Fragment.NotNull] = Fragment.Col.Col("title", SqlValue.Str)
    pub def body(): Fragment.Col[PostsTable, String, Fragment.NotNull] = Fragment.Col.Col("body", SqlValue.Str)
    pub def published(): Fragment.Col[PostsTable, Bool, Fragment.NotNull] = Fragment.Col.Col("published", SqlValue.Bool)
    pub def tags(): Fragment.Col[PostsTable, List[String], Fragment.NotNull] = Fragment.Col.Col("tags", SqlValue.StrArray)
    pub def createdAt(): Fragment.Col[PostsTable, Timestamp.Timestamp, Fragment.NotNull] = Fragment.Col.Col("created_at", SqlValue.ofTimestamp)
    pub def views(): Fragment.Col[PostsTable, Int32, Fragment.NotNull] = Fragment.Col.Col("views", SqlValue.Int32)
    pub def updatedAt(): Fragment.Col[PostsTable, Timestamp.Timestamp, Fragment.NotNull] = Fragment.Col.Col("updated_at", SqlValue.ofTimestamp)

    ///
    /// Named constraints from the DDL. Translate violations in the function passed to `onConstraint`; a missing case is a compile error.
    ///
    pub enum Constraint with Eq, Order, ToString {
        /// posts_user_id_fkey (foreign key)
        case UserIdFkey
    }

    ///
    /// Runs a write against this table, translating constraint violations with `translate`.
    /// `translate` either returns a value of the same type as the thunk, or raises a domain error effect. For a case you do not translate, call `raiseConstraint`.
    /// A violation whose name is not in the enum, and any DbErr other than a constraint violation, pass through. Call it outside a Tx (so that no COMMIT is sent to a Tx the violation aborted).
    ///
    pub def onConstraint(translate: Constraint -> a \ ef1, thunk: Unit -> a \ ef2): a \ ef1 + ef2 + {TransientDbErr, DbErr} =
        DbError.onConstraintWith(constraintOf, translate, thunk)

    ///
    /// Turns a constraint you do not translate back into the original DbErr. Use it inside the function passed to `onConstraint`.
    ///
    pub def raiseConstraint(constraint: Constraint): a \ DbErr = match constraint {
        case Constraint.UserIdFkey => DbErr.foreignKeyViolation("posts_user_id_fkey")
    }

    def constraintOf(name: String): Option[Constraint] = match name {
        case "posts_user_id_fkey" => Some(Constraint.UserIdFkey)
        case _ => None
    }

}

///
/// Table users. The enum is the tag the fragment DSL puts on the type; the functions are the columns.
///
mod UsersTable {

    use Sqlfx.Date
    use Sqlfx.DbErr
    use Sqlfx.DbError
    use Sqlfx.DbRead
    use Sqlfx.DbWrite
    use Sqlfx.Decoder
    use Sqlfx.Fragment
    use Sqlfx.RawSql
    use Sqlfx.Sql
    use Sqlfx.SqlValue
    use Sqlfx.Timestamp
    use Sqlfx.TransientDbErr
    use Sqlfx.Tx
    use Sqlfx.Uuid
    use Sqlfx.SqlValue.SqlValue
    use Sqlfx.Decoder.Decoder

    pub enum UsersTable {
        case UsersTable
    }

    pub def id(): Fragment.Col[UsersTable, Int64, Fragment.NotNull] = Fragment.Col.Col("id", SqlValue.Int64)
    pub def name(): Fragment.Col[UsersTable, String, Fragment.NotNull] = Fragment.Col.Col("name", SqlValue.Str)
    pub def email(): Fragment.Col[UsersTable, String, Fragment.Nullable] = Fragment.Col.Col("email", SqlValue.Str)
    pub def role(): Fragment.Col[UsersTable, String, Fragment.NotNull] = Fragment.Col.Col("role", SqlValue.Str)
    pub def createdAt(): Fragment.Col[UsersTable, Timestamp.Timestamp, Fragment.NotNull] = Fragment.Col.Col("created_at", SqlValue.ofTimestamp)
    pub def deletedAt(): Fragment.Col[UsersTable, Timestamp.Timestamp, Fragment.Nullable] = Fragment.Col.Col("deleted_at", SqlValue.ofTimestamp)

    ///
    /// Named constraints from the DDL. Translate violations in the function passed to `onConstraint`; a missing case is a compile error.
    ///
    pub enum Constraint with Eq, Order, ToString {
        /// users_email_key (unique)
        case EmailKey
        /// users_name_length (check)
        case NameLength
    }

    ///
    /// Runs a write against this table, translating constraint violations with `translate`.
    /// `translate` either returns a value of the same type as the thunk, or raises a domain error effect. For a case you do not translate, call `raiseConstraint`.
    /// A violation whose name is not in the enum, and any DbErr other than a constraint violation, pass through. Call it outside a Tx (so that no COMMIT is sent to a Tx the violation aborted).
    ///
    pub def onConstraint(translate: Constraint -> a \ ef1, thunk: Unit -> a \ ef2): a \ ef1 + ef2 + {TransientDbErr, DbErr} =
        DbError.onConstraintWith(constraintOf, translate, thunk)

    ///
    /// Turns a constraint you do not translate back into the original DbErr. Use it inside the function passed to `onConstraint`.
    ///
    pub def raiseConstraint(constraint: Constraint): a \ DbErr = match constraint {
        case Constraint.EmailKey => DbErr.uniqueViolation("users_email_key")
        case Constraint.NameLength => DbErr.checkViolation("users_name_length")
    }

    def constraintOf(name: String): Option[Constraint] = match name {
        case "users_email_key" => Some(Constraint.EmailKey)
        case "users_name_length" => Some(Constraint.NameLength)
        case _ => None
    }

}
